version: '3'
services:
  bot:
    build: .
    container_name: posvyat_tg_bot
    command: python run_pooling.py
    volumes:
      - .:/code
#    env_file:
#      - ./.env
    depends_on:
      - web